import SwiftUI

struct QuizView: View {
    var body: some View {
        ZStack {
            SoftHexBackground()
            Text("Quiz")
                .font(.largeTitle).bold()
        }
    }
}

